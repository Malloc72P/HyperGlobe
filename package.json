{
  "name": "hyperglobe-actual",
  "private": true,
  "scripts": {
    "hg": "pnpm --filter @hyperglobe/cli hg",
    "build": "turbo build",
    "build:map": "pnpm --filter @hyperglobe/maps build:map",
    "dev": "turbo dev",
    "start": "turbo start",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "test": "turbo test",
    "check-types": "turbo check-types"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "tsx": "^4.19.2",
    "turbo": "^2.5.4",
    "typescript": "5.5.4"
  },
  "packageManager": "pnpm@10.11.0",
  "engines": {
    "node": ">=22.20"
  }
}
